This example uses the language translation service to identify the language of a given text.

Within this example four inject nodes are used to inject a text into the language service. Currentluy four languages are used

*German
*English
*French
*Turkey

The service returns a probability for the identify language.